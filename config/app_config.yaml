# config/app_config.yaml
# Ana uygulama konfigürasyonu

camera:
  source: 0                          # 0 = varsayılan USB kamera, "rtsp://..." için URL girin
  width: 1280
  height: 720
  fps: 30
  reconnect_delay_seconds: 3.0
  max_reconnect_attempts: 10

pipeline:
  default_camera_id: "camera-0"
  target_fps: 15.0                   # Inference FPS hedefi
  auto_capture_to_queue_enabled: false   # true olursa dashboard açıkken periyodik otomatik kayıt yapılır
  low_confidence_feedback_enabled: false # true olursa düşük confidence frame'leri otomatik queue'ya yazılır
  capture_save_every_n_seconds: 1500000.0 # Kaç saniyede bir frame dataset adayına eklenir

mqtt:
  host: "${MQTT_HOST:-mosquitto}"
  port: 1883
  base_topic: "vitra/ai"

modbus:
  enabled: false
  host: "${MODBUS_HOST:-localhost}"
  port: 502
  alarm_coil_address: 0

data_dirs:
  raw_captures: "data/raw_captures"
  labeling_queue: "data/labeling_queue"
  dataset: "data/dataset"
  model_registry: "data/models/registry"
  production_model: "data/models/production"

api:
  host: "0.0.0.0"
  port: 8000
  workers: 1

cameraIps:
  camera-0: "rtsp://admin:Vitra2026@192.168.1.100:554/stream"
